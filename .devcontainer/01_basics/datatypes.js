"use strict"; // tret all JS code as newer version
// alert(3+3) we are using node js not browser. So this we will not work here only

let name = "Akash"
let age = 20
let isLoogedIn = false
// number => 2 to 53 power
// bigint
// String => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique
// object
console.log(typeof age);
console.log(name);

// type of null is a object data type and type of undefined is a undefined data type